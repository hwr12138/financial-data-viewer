# Copy to .streamlit/secrets.toml and fill in real values. That file is gitignored and must
# stay that way — see SPEC §8.
#
# On Streamlit Community Cloud these are entered in the app's settings UI instead, never
# committed.

API_BASE_URL = "https://fundamentals.example.com"
API_KEY = "replace-me"
